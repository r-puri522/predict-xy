# predict-xy
Comparative machine learning analysis using Support Vector Machines (SVM), Logistic Regression, and k-Nearest Neighbors (KNN) to predict a binary outcome variable from a set of numeric features. 
